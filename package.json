{
  "name": "clawd-app",
  "version": "0.1.0",
  "description": "Claw'd App - All-in-one desktop app: clawd-chat server + UI + worker manager",
  "main": "dist/electron/main.cjs",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "dev:ui": "cd packages/ui && bun run dev",
    "dev:electron": "bun run build:electron && NODE_ENV=development electron dist/electron/main.cjs",
    "build:ui": "cd packages/ui && bun install && bun run build",
    "build:embed": "bun run scripts/embed-ui.ts",
    "build:electron": "cd electron && npx tsc -p tsconfig.json && cd ../dist/electron && mv main.js main.cjs && mv preload.js preload.cjs",
    "build:server": "mkdir -p dist/server && bun run build:embed && bun build src/index.ts --compile --minify --target=bun --outfile dist/server/clawd-app",
    "compile:linux-x64": "bun build src/index.ts --compile --minify --target=bun-linux-x64 --outfile dist/server/clawd-app-linux-x64",
    "compile:linux-arm64": "bun build src/index.ts --compile --minify --target=bun-linux-arm64 --outfile dist/server/clawd-app-linux-arm64",
    "compile:darwin-arm64": "bun build src/index.ts --compile --minify --target=bun-darwin-arm64 --outfile dist/server/clawd-app-darwin-arm64",
    "compile:darwin-x64": "bun build src/index.ts --compile --minify --target=bun-darwin-x64 --outfile dist/server/clawd-app-darwin-x64",
    "compile:windows-x64": "bun build src/index.ts --compile --minify --target=bun-windows-x64 --outfile dist/server/clawd-app-windows-x64.exe",
    "build:server:linux-x64": "mkdir -p dist/server && bun run build:embed && bun run compile:linux-x64",
    "build:server:linux-arm64": "mkdir -p dist/server && bun run build:embed && bun run compile:linux-arm64",
    "build:server:darwin-arm64": "mkdir -p dist/server && bun run build:embed && bun run compile:darwin-arm64",
    "build:server:darwin-x64": "mkdir -p dist/server && bun run build:embed && bun run compile:darwin-x64",
    "build:server:windows-x64": "mkdir -p dist/server && bun run build:embed && bun run compile:windows-x64",
    "build:server:all": "mkdir -p dist/server && bun run build:embed && bun run compile:linux-x64 && bun run compile:linux-arm64 && bun run compile:darwin-arm64 && bun run compile:darwin-x64 && bun run compile:windows-x64",
    "build": "bun run build:ui && bun run build:server && bun run build:electron",
    "build:all": "bun run build:ui && bun run build:server:all",
    "build:linux": "bun run build:ui && bun run build:server:linux-x64",
    "build:macos": "bun run build:ui && bun run build:server:darwin-arm64",
    "pack": "bun run build && npx electron-builder --dir",
    "dist": "bun run build && npx electron-builder",
    "dist:mac": "bun run build:macos && npx electron-builder --mac",
    "dist:linux": "bun run build:linux && npx electron-builder --linux",
    "dist:win": "bun run build && npx electron-builder --win",
    "install:local": "mkdir -p ~/.clawd/bin && cp dist/server/clawd-app ~/.clawd/bin/",
    "format": "bunx biome format --write .",
    "format:check": "bunx biome format .",
    "lint": "bunx biome lint .",
    "lint:fix": "bunx biome lint --write .",
    "check": "bunx biome check --write .",
    "clean": "rm -rf dist release node_modules packages/ui/node_modules packages/ui/dist src/embedded-ui.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@types/bun": "latest",
    "electron": "^35.1.5",
    "electron-builder": "^26.0.12",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "private": true
}
